package project.transito.controller;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;

import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RestController;

import project.transito.models.AgenteTransito;
import project.transito.models.Via;
import project.transito.service.AgenteTransitoService;
import project.transito.service.ViaService;

@RestController
@CrossOrigin(origins = "*")
public class Controller {
	
	
	@Autowired
	public ViaService viaService;
	
	@Autowired
	public AgenteTransitoService agenteTransitoService;

	
	//--- metodos de agentes de transito---//
	@PostMapping("/addAgente")
	public boolean addAgente( AgenteTransito agenteTransito)
	{
		boolean validar = agenteTransitoService.add(agenteTransito);
				
				return validar;
	}
	
	@DeleteMapping("/deleteAgente/{codigo}")
	public boolean deleteAgente(AgenteTransito agenteTransito)
	{
		boolean validar = agenteTransitoService.delete(agenteTransito.getCodigo());
		return validar;
	}
	@PutMapping("/updateAgente")
	public boolean updateAgente(AgenteTransito agenteTransito)
	{
		boolean validar = agenteTransitoService.update(agenteTransito);
		return validar;
	}
	@GetMapping("/searchAgente/{codigo}")
	public AgenteTransito searchAgente(AgenteTransito agenteTransito2)
	{
		AgenteTransito agenteTransito = agenteTransitoService.search(agenteTransito2.getCodigo());
		return agenteTransito;
	}
	
	@GetMapping("/listAgente")
	public List<AgenteTransito> listAgente()
	{
		List<AgenteTransito> list = agenteTransitoService.list();
		
		return list;
	}
	
	//---metodos de via-----//
	
	
	@PostMapping("/addVia")
	public boolean addVia( Via via)
	{
		boolean validar = viaService.add(via);
				
				return validar;
	}
	
	@DeleteMapping("/deleteVia/{identificador}")
	public boolean deleteVia(Via via)
	{
		boolean validar = viaService.delete(via.getIdentificador());
		return validar;
	}
	@PutMapping("/updateVia")
	public boolean updateVia(Via via)
	{
		boolean validar = viaService.update(via);
		return validar;
	}
	@GetMapping("/searchAgente/{identificador}")
	public Via searchVia(Via via)
	{
		Via v= viaService.search(via.getIdentificador());
		return v;
	}
	
	@GetMapping("/listVia")
	public List<Via> listVia()
	{
		List<Via> list = viaService.list();
		
		return list;
	}
	

}
